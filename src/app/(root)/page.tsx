import { Typography } from "@/components/dashboard-ui/typography";

export default function Home() {
  return (
    <div>
      <Typography variant="h1">Grid</Typography>
      <Typography variant="h2">Grid</Typography>
      <Typography variant="p">flex orqali yangi navigation bar ochish kerak</Typography>
    </div>
  );
}
